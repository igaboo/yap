Yap Multi-Platform Release — TODO
==================================

DONE (this session)
-------------------
[x] Native Swift overlay sidecar for macOS (NSPanel + SwiftUI)
[x] IPC protocol (stdin/stdout newline-delimited JSON)
[x] Sidecar build script (build-sidecar.sh) + build.rs integration
[x] Platform-specific Tauri configs (tauri.macos.conf.json)
[x] Deleted all legacy hover tracking code (event monitors, pill bounds, etc.)
[x] WebView overlay destroyed on macOS at startup (clean build)
[x] Sidecar binary bundled in Contents/MacOS/ via externalBin
[x] Native Windows overlay path compiles with Win32 layered window support
[x] Root-level legacy Swift app removed; Tauri app is canonical

BEFORE RELEASE — macOS
----------------------
[ ] Full pipeline end-to-end test: hotkey → record → transcribe → format → paste
[ ] Test all overlay states: idle, recording, processing, noSpeech, error
[ ] Test onboarding flow through sidecar
[ ] Test hands-free: pause, resume, stop, elapsed timer
[ ] Test settings changes propagating to sidecar (gradient, visibility, hotkey)
[ ] Code signing: verify sidecar binary gets signed in release builds
[ ] Universal binary (arm64 + x86_64) for Intel Mac support
[ ] CI/CD: GitHub Actions macOS runner needs Xcode/Swift for sidecar build

BEFORE RELEASE — Windows
-------------------------
[ ] Test basic pipeline: hotkey → record → transcribe → format → paste
[ ] Test native Win32 overlay (recording, processing, error states)
[ ] Test overlay click-through/hover behavior on a real Windows machine
[ ] CI/CD: GitHub Actions Windows runner build test

FUTURE — General
----------------
[ ] Remove overlay window from tauri.conf.json entirely once both platforms have native sidecars
[ ] Consider single Rust sidecar binary (cross-platform) instead of Swift + future Windows binary
[ ] Investigate: could the Rust sidecar use platform-native rendering (AppKit on macOS, Win32 on Windows)?
